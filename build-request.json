{
  "kind": "build_request",
  "title": "Add filter/search option to Dashboard",
  "projectName": "District Status Page",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Add a search/filter bar to the public Dashboard page that allows users to filter visible incidents by keyword (matching title, description, or affected service), severity, and status. The filter controls should update the displayed incident list in real time without requiring a page reload.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-7"
        ],
        "quotes": [
          "Ok lets add the filter/search option again."
        ]
      },
      "acceptanceCriteria": [
        "A text input field is visible on the Dashboard for keyword search",
        "Typing in the search field filters incidents whose title, description, or affected service contains the entered text (case-insensitive)",
        "A severity filter control (e.g., dropdown or button group) allows filtering by critical, major, minor, or informational",
        "A status filter control allows filtering by investigating, identified, monitoring, or resolved",
        "Filters can be combined (e.g., keyword + severity + status applied simultaneously)",
        "Both the active incidents section and the resolved incidents section are filtered independently",
        "Clearing all filters restores the full incident list",
        "Filter controls are styled consistently with the existing Dashboard design and theme"
      ]
    }
  ],
  "constraints": [
    "Do not modify backend logic — filtering must be done client-side on already-fetched incident data",
    "Do not edit any files under frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, or frontend/src/components/ui"
  ],
  "nonGoals": [
    "Backend search or query endpoints",
    "Saved/persistent filter preferences",
    "Pagination or infinite scroll",
    "Changes to AdminPanel"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}