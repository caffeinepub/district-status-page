{
  "frontend/src/index.css": "Dark/light system-monitor theme with OKLCH color tokens — `:root` now defines a proper light theme (light backgrounds, dark text) and `.dark` retains the original dark theme; includes semantic severity/status colors, utility classes, and font imports.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/JetBrains Mono font families, custom severity and status color tokens mapped to OKLCH CSS variables, card shadow utilities, and standard shadcn/ui token mappings.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — all queries use `enabled: !!actor && !isFetching` to ensure they only fire when the actor is fully initialized, preventing race-condition errors on initial load.",
  "frontend/src/components/StatusBadge.tsx": "Renders a color-coded outline badge for incident status (investigating/identified/monitoring/resolved) using OKLCH colors and monospace uppercase typography.",
  "frontend/src/components/SeverityBadge.tsx": "Renders a color-coded outline badge with a colored dot indicator for incident severity levels (critical/major/minor/informational), with a pulsing animation for critical severity.",
  "frontend/src/components/IncidentCard.tsx": "Collapsible incident card showing title, severity/status badges, affected service, timestamps (relative and absolute), description, and a chronological update timeline with visual connector lines.",
  "frontend/src/components/Layout.tsx": "Full-page layout wrapper with sticky header (logo, nav links, ThemeToggle button, and a conditional logout button shown when on /admin and authenticated), main content slot, and footer with dynamic year and caffeine.ai attribution.",
  "frontend/src/pages/Dashboard.tsx": "Public status dashboard with search/filter controls (status and severity selects), loading/error/content states, and active/resolved incident sections — uses the named `{ IncidentCard }` import.",
  "frontend/src/components/CreateIncidentForm.tsx": "React Hook Form-based form for creating new incidents — calls `checkIdUsed.mutateAsync(data.id)` (via the `useMutation` result from `useIsIdAlreadyUsed`) instead of invoking it as a plain function, and disables the submit button while either mutation is pending.",
  "frontend/src/components/AddUpdateForm.tsx": "Form for appending a timestamped update message to an existing incident, with an incident selector dropdown showing current status context.",
  "frontend/src/components/ChangeStatusForm.tsx": "Form for changing an incident's status via incident selector and status dropdown, showing the current status for context before submission.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel with Internet Identity auth gate (initializing/login screens) and a tabbed interface (Create/Add Update/Change Status/Delete) for authenticated users — passes `allIncidents` array to `AddUpdateForm` and `ChangeStatusForm` as required, and handles delete confirmation dialogs with toast notifications.",
  "frontend/src/App.tsx": "Root app component with ThemeProvider → QueryClientProvider → RouterProvider nesting; defines all routes (/ for Dashboard, /admin for AdminPanel) using a named import for AdminPanel and a shared Layout wrapper via Outlet.",
  "frontend/src/hooks/useTheme.ts": "Thin wrapper around next-themes' useTheme hook that exposes isDark, theme, and toggleTheme — delegates all theme state and persistence to next-themes instead of a custom localStorage implementation.",
  "frontend/src/components/ThemeToggle.tsx": "A small icon button component that uses the useTheme hook to display a Sun icon in dark mode and a Moon icon in light mode, toggling between themes on click with an accessible aria-label."
}